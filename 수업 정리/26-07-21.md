# ROS 2 패키지 생성 및 실행 실습

- **수업 날짜:** 2026-07-21
- **교재 범위:** p.256-267
- **개발 환경:** WSL2, Ubuntu 24.04, ROS 2 Jazzy

---

# 1. ROS 2 파일 시스템

현재 실습 워크스페이스는 다음 위치에 있다.

```text
~/2026_manipulator/khj_ws
```

기본 구조는 다음과 같다.

```text
khj_ws/
├── src/       # 직접 작성하거나 내려받은 패키지
├── build/     # 빌드 중간 결과
├── install/   # 실행에 필요한 설치 결과와 환경 설정
└── log/       # 빌드 로그
```

`src` 폴더에는 직접 작성하는 ROS 2 패키지를 넣는다.

```text
khj_ws/src/khj_basic
```

> `build`, `install`, `log` 폴더는 `colcon build`를 실행하면 자동으로 만들어진다.

---

# 2. Python ROS 2 패키지 생성

## 8.1 워크스페이스의 `src` 폴더로 이동

```bash
cd ~/2026_manipulator/khj_ws/src
```

## 8.2 패키지 생성

```bash
ros2 pkg create --build-type ament_python khj_basic
```

### 명령어 의미

| 항목 | 의미 |
|---|---|
| `ros2 pkg create` | 새로운 ROS 2 패키지 생성 |
| `--build-type ament_python` | Python 패키지 형식 사용 |
| `khj_basic` | 생성할 패키지 이름 |

> 현재 `khj_basic` 패키지가 이미 존재하므로 이 명령을 다시 실행할 필요는 없다.

---

# 3. `khj_basic` 패키지 구조

현재 패키지의 핵심 구조는 다음과 같다.

```text
khj_basic/
├── khj_basic/
│   ├── __init__.py
│   └── simple_pub.py
├── resource/
│   └── khj_basic
├── test/
├── package.xml
├── setup.cfg
└── setup.py
```

## 9.1 파일별 역할

| 파일 또는 폴더 | 역할 |
|---|---|
| `khj_basic/` | Python 소스 코드가 들어가는 내부 모듈 폴더 |
| `__init__.py` | 해당 폴더를 Python 패키지로 인식시킴 |
| `simple_pub.py` | 이번 실습에서 작성한 Python 실행 파일 |
| `resource/khj_basic` | ament 인덱스가 패키지를 찾을 때 사용하는 파일 |
| `test/` | 코드 검사와 테스트 파일이 들어가는 폴더 |
| `package.xml` | 패키지 이름, 버전, 관리자, 라이선스, 의존성 정보 |
| `setup.py` | Python 패키지 설치 및 실행 파일 등록 설정 |
| `setup.cfg` | ROS 2 실행 파일의 설치 위치 설정 |

GitHub에서 패키지 폴더로 바로 이동하려면 다음 상대 링크를 사용할 수 있다.

- [`khj_basic` 패키지](../khj_ws/src/khj_basic)
- [`simple_pub.py`](../khj_ws/src/khj_basic/khj_basic/simple_pub.py)

---

# 4. 첫 번째 Python 실행 파일 작성

파일 위치:

```text
~/2026_manipulator/khj_ws/src/khj_basic/khj_basic/simple_pub.py
```

작성한 코드:

```python
def main():
    print("첫번째 프로그램입니다.")


if __name__ == '__main__':
    main()
```

## 10.1 코드 설명

### `main()` 함수

```python
def main():
```

프로그램을 실행했을 때 수행할 내용을 함수로 정의한다.

### 문자열 출력

```python
print("첫번째 프로그램입니다.")
```

터미널에 문자열을 출력한다.

### Python 파일 직접 실행 확인

```python
if __name__ == '__main__':
    main()
```

현재 파일을 직접 실행했을 때 `main()` 함수를 호출한다.

---

# 5. `setup.py`에 실행 파일 등록

Python 파일을 `ros2 run`으로 실행하려면 `setup.py`의 `console_scripts`에 등록해야 한다.

```python
entry_points={
    'console_scripts': [
        'simple_pub = khj_basic.simple_pub:main',
    ],
},
```

## 11.1 등록 형식

```text
실행 파일 이름 = Python 모듈 경로:실행 함수
```

현재 설정을 분리하면 다음과 같다.

| 부분 | 값 | 의미 |
|---|---|---|
| 실행 파일 이름 | `simple_pub` | `ros2 run`에서 사용할 이름 |
| 패키지 모듈 | `khj_basic` | 내부 Python 패키지 폴더 |
| Python 파일 | `simple_pub` | `simple_pub.py` |
| 실행 함수 | `main` | 파일 안의 `main()` 함수 |

따라서 다음 명령으로 실행할 수 있다.

```bash
ros2 run khj_basic simple_pub
```

---

# 6. `package.xml`

`package.xml`에는 패키지에 대한 기본 정보가 들어 있다.

현재 주요 내용:

```xml
<name>khj_basic</name>
<version>0.0.0</version>
<maintainer email="khjoo990408@gmail.com">khjoo</maintainer>

<export>
  <build_type>ament_python</build_type>
</export>
```

### 주요 태그

| 태그 | 의미 |
|---|---|
| `<name>` | 패키지 이름 |
| `<version>` | 패키지 버전 |
| `<description>` | 패키지 설명 |
| `<maintainer>` | 패키지 관리자 |
| `<license>` | 라이선스 |
| `<depend>` | 빌드와 실행에 필요한 의존성 |
| `<test_depend>` | 테스트에 필요한 의존성 |
| `<build_type>` | 패키지 빌드 형식 |

---

# 7. `setup.cfg`와 `setup.py`

## 7.1 `setup.cfg`

`setup.cfg`는 Python 실행 파일이 설치될 위치를 지정하는 파일이다.

현재 내용은 다음과 같다.

```ini
[develop]
script_dir=$base/lib/khj_basic

[install]
install_scripts=$base/lib/khj_basic
```

실행 파일은 다음 경로에 설치된다.

```text
install/khj_basic/lib/khj_basic/
```

ROS 2는 이 경로에서 실행 파일을 찾아 `ros2 run` 명령으로 실행한다.

---

## 7.2 `setup.py`

Python 파일을 `ros2 run`으로 실행하려면 `setup.py`의 `console_scripts`에 등록해야 한다.

현재 등록된 실행 파일은 다음과 같다.

```python
entry_points={
    'console_scripts': [
        "simple_pub = khj_basic.simple_pub:main",
        "class_pub = khj_basic.class_pub:main",
        "class_sub = khj_basic.class_sub:main"
    ],
},
```

### 등록 형식

```text
실행 파일 이름 = Python 모듈 경로:실행 함수
```

각 항목의 의미는 다음과 같다.

| 실행 파일 이름 | Python 파일 | 실행 함수 |
|---|---|---|
| `simple_pub` | `khj_basic/simple_pub.py` | `main()` |
| `class_pub` | `khj_basic/class_pub.py` | `main()` |
| `class_sub` | `khj_basic/class_sub.py` | `main()` |

따라서 다음과 같이 실행한다.

```bash
ros2 run khj_basic simple_pub
ros2 run khj_basic class_pub
ros2 run khj_basic class_sub
```

> 실행 파일을 새로 추가하거나 `setup.py`를 수정한 경우에는 반드시 패키지를 다시 빌드해야 한다.

---

# 8. 패키지 빌드

## 8.1 패키지가 있는 경로 확인

현재 패키지는 다음 위치에 있다.

```text
~/2026_manipulator/khj_ws/src/khj_basic
```

빌드는 패키지 폴더 안이 아니라 다음 경로에서 실행한다.

```bash
cd ~/2026_manipulator/khj_ws
```

---

## 8.2 특정 패키지만 빌드

```bash
colcon build --symlink-install --packages-select khj_basic
```

### 옵션 설명

| 옵션 | 의미 |
|---|---|
| `colcon build` | ROS 2 패키지 빌드 |
| `--symlink-install` | Python 소스 수정 내용을 빠르게 반영 |
| `--packages-select khj_basic` | `khj_basic` 패키지만 선택하여 빌드 |

빌드에 성공하면 다음과 비슷하게 출력된다.

```text
Starting >>> khj_basic
Finished <<< khj_basic
Summary: 1 package finished
```

---

## 8.3 전체 패키지 빌드

```bash
colcon build --symlink-install
```

특정 패키지만 빠르게 빌드할 때는 다음 단축 명령을 사용할 수 있다.

```bash
cbp khj_basic
```

`cbp`는 다음 명령을 줄여 놓은 것이다.

```bash
colcon build --symlink-install --packages-select khj_basic
```

전체 패키지를 빌드하는 단축 명령:

```bash
cb
```

등록한 별칭:

```bash
alias cbp='colcon build --symlink-install --packages-select'
alias cb='cd ~/2026_manipulator/khj_ws && colcon build --symlink-install'
```

---

# 9. 빌드 환경 적용 및 실행

## 9.1 환경 설정 적용

패키지를 빌드한 후 현재 터미널이 빌드 결과를 찾을 수 있도록 환경 설정을 적용한다.

```bash
source ~/2026_manipulator/khj_ws/install/local_setup.bash
```

ROS 2 Jazzy 환경까지 함께 불러오는 순서는 다음과 같다.

```bash
source /opt/ros/jazzy/setup.bash
source ~/2026_manipulator/khj_ws/install/local_setup.bash
```

`.bashrc`에 위 명령을 저장했다면 다음 명령으로 다시 적용한다.

```bash
source ~/.bashrc
```

단축 명령:

```bash
sb
```

등록한 별칭:

```bash
alias sb='source ~/.bashrc'
```

> 첫 번째 빌드 전에는 `install/local_setup.bash` 파일이 존재하지 않는다. 먼저 `colcon build`를 실행한 뒤 불러와야 한다.

---

## 9.2 등록된 실행 파일 확인

```bash
ros2 pkg executables khj_basic
```

예상 결과:

```text
khj_basic class_pub
khj_basic class_sub
khj_basic simple_pub
```

---

## 9.3 패키지 실행

첫 번째 Python 프로그램 실행:

```bash
ros2 run khj_basic simple_pub
```

Publisher 노드 실행:

```bash
ros2 run khj_basic class_pub
```

Subscriber 노드 실행:

```bash
ros2 run khj_basic class_sub
```

---

# 10. 클래스 기반 Publisher 노드 작성

파일 위치:

```text
~/2026_manipulator/khj_ws/src/khj_basic/khj_basic/class_pub.py
```

Publisher는 `message` 토픽으로 문자열 메시지를 발행한다.

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String


class M_pub(Node):

    def __init__(self):
        super().__init__('message_pub')

        # 1초마다 timer_callback 함수를 실행
        self.create_timer(1.0, self.timer_callback)

        # String 메시지를 message 토픽으로 발행하는 Publisher 생성
        self.pub = self.create_publisher(
            String,
            'message',
            10
        )

        self.count = 0

    def timer_callback(self):
        msg = String()
        msg.data = f'첫번째 프로그램 {self.count}'

        self.get_logger().info(msg.data)
        self.pub.publish(msg)

        self.count += 1


def main(args=None):
    rclpy.init(args=args)

    node = M_pub()

    try:
        rclpy.spin(node)

    except KeyboardInterrupt:
        node.get_logger().info('KeyboardInterrupt')

    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

---

## 10.1 코드 구성

### ROS 2 라이브러리 불러오기

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String
```

| 코드 | 의미 |
|---|---|
| `rclpy` | Python에서 ROS 2를 사용하기 위한 라이브러리 |
| `Node` | ROS 2 노드를 생성하기 위한 클래스 |
| `String` | 문자열 데이터를 전송하기 위한 메시지 자료형 |

---

### 노드 클래스 정의

```python
class M_pub(Node):
```

`Node` 클래스를 상속하여 Publisher 노드를 만든다.

---

### 노드 이름 설정

```python
super().__init__('message_pub')
```

실행되는 노드의 이름은 다음과 같다.

```text
/message_pub
```

노드 목록 확인:

```bash
ros2 node list
```

---

### Timer 생성

```python
self.create_timer(1.0, self.timer_callback)
```

1초마다 `timer_callback()` 함수가 실행된다.

```text
Timer
  ↓ 1초마다 실행
timer_callback()
```

---

### Publisher 생성

```python
self.pub = self.create_publisher(
    String,
    'message',
    10
)
```

| 인수 | 의미 |
|---|---|
| `String` | 발행할 메시지 자료형 |
| `message` | 토픽 이름 |
| `10` | QoS 메시지 대기열 크기 |

생성되는 토픽:

```text
/message
```

---

### 메시지 생성 및 발행

```python
msg = String()
msg.data = f'첫번째 프로그램 {self.count}'
self.pub.publish(msg)
```

`String` 메시지 객체를 생성한 뒤 `data` 필드에 문자열을 넣고 발행한다.

출력 예시:

```text
첫번째 프로그램 0
첫번째 프로그램 1
첫번째 프로그램 2
```

---

### `spin()`

```python
rclpy.spin(node)
```

노드가 종료되지 않고 Timer, Subscription 등의 콜백을 계속 처리하도록 한다.

`Ctrl + C`를 입력하면 `KeyboardInterrupt`가 발생하여 노드를 종료한다.

---

# 11. 클래스 기반 Subscriber 노드 작성

파일 위치:

```text
~/2026_manipulator/khj_ws/src/khj_basic/khj_basic/class_sub.py
```

Subscriber는 Publisher가 `/message` 토픽으로 발행한 문자열을 수신한다.

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String


class M_sub(Node):

    def __init__(self):
        super().__init__('message_sub')

        # message 토픽을 구독하는 Subscription 생성
        self.create_subscription(
            String,
            'message',
            self.sub_callback,
            10
        )

    def sub_callback(self, msg: String):
        self.get_logger().info(msg.data)


def main(args=None):
    rclpy.init(args=args)

    node = M_sub()

    try:
        rclpy.spin(node)

    except KeyboardInterrupt:
        node.get_logger().info('KeyboardInterrupt')

    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

> 기존 실습 코드에는 Subscriber 클래스 이름과 노드 이름이 `M_pub`, `message_pub`으로 작성되어 있었다. Publisher와 구분하기 쉽도록 `M_sub`, `message_sub`로 정리하였다.

---

## 11.1 Subscription 생성

```python
self.create_subscription(
    String,
    'message',
    self.sub_callback,
    10
)
```

| 인수 | 의미 |
|---|---|
| `String` | 수신할 메시지 자료형 |
| `message` | 구독할 토픽 이름 |
| `self.sub_callback` | 메시지 수신 시 실행할 함수 |
| `10` | QoS 메시지 대기열 크기 |

Publisher와 Subscriber의 메시지 자료형과 토픽 이름이 같아야 통신할 수 있다.

```text
Publisher                     Subscriber
String                        String
/message      ─────────▶      /message
```

---

## 11.2 Subscriber 콜백 함수

```python
def sub_callback(self, msg: String):
    self.get_logger().info(msg.data)
```

Publisher가 메시지를 발행하면 `sub_callback()` 함수가 실행된다.

수신한 `String` 메시지의 실제 문자열은 다음 필드에 들어 있다.

```python
msg.data
```

---

# 12. Publisher와 Subscriber 통신 테스트

Publisher와 Subscriber는 서로 다른 터미널에서 실행한다.

## 12.1 첫 번째 터미널: Publisher 실행

```bash
ros2 run khj_basic class_pub
```

예상 출력:

```text
[INFO] [message_pub]: 첫번째 프로그램 0
[INFO] [message_pub]: 첫번째 프로그램 1
[INFO] [message_pub]: 첫번째 프로그램 2
```

Publisher는 1초마다 `/message` 토픽으로 문자열을 발행한다.

---

## 12.2 두 번째 터미널: Subscriber 실행

```bash
ros2 run khj_basic class_sub
```

예상 출력:

```text
[INFO] [message_sub]: 첫번째 프로그램 0
[INFO] [message_sub]: 첫번째 프로그램 1
[INFO] [message_sub]: 첫번째 프로그램 2
```

Subscriber는 `/message` 토픽을 구독하여 수신한 문자열을 출력한다.

---

## 12.3 통신 구조

```text
/message_pub
     │
     │ std_msgs/msg/String
     ▼
  /message
     │
     ▼
/message_sub
```

- Publisher 노드: `/message_pub`
- Subscriber 노드: `/message_sub`
- 토픽 이름: `/message`
- 메시지 자료형: `std_msgs/msg/String`
- 발행 주기: 1초

---

# 13. 실행 중인 노드와 토픽 확인

## 13.1 노드 목록 확인

```bash
ros2 node list
```

예상 결과:

```text
/message_pub
/message_sub
```

---

## 13.2 토픽 목록 확인

```bash
ros2 topic list
```

예상 결과에 다음 토픽이 포함된다.

```text
/message
/parameter_events
/rosout
```

---

## 13.3 토픽 메시지 직접 확인

```bash
ros2 topic echo /message
```

예상 결과:

```yaml
data: 첫번째 프로그램 0
---
data: 첫번째 프로그램 1
---
data: 첫번째 프로그램 2
---
```

---

## 13.4 토픽 자료형 확인

```bash
ros2 topic type /message
```

예상 결과:

```text
std_msgs/msg/String
```

---

## 13.5 토픽 상세 정보 확인

```bash
ros2 topic info /message
```

예상 결과:

```text
Type: std_msgs/msg/String
Publisher count: 1
Subscription count: 1
```

더 자세한 정보를 확인하려면:

```bash
ros2 topic info /message --verbose
```

---

## 13.6 메시지 구조 확인

```bash
ros2 interface show std_msgs/msg/String
```

결과:

```text
string data
```

`String` 메시지는 문자열을 저장하는 `data` 필드 하나로 구성된다.

---

# 14. `setup.py` 수정 후 다시 빌드하기

`class_pub.py`와 `class_sub.py`를 작성한 뒤 `setup.py`에 실행 파일을 등록한다.

```python
entry_points={
    'console_scripts': [
        "simple_pub = khj_basic.simple_pub:main",
        "class_pub = khj_basic.class_pub:main",
        "class_sub = khj_basic.class_sub:main"
    ],
},
```

그다음 다시 빌드한다.

```bash
cd ~/2026_manipulator/khj_ws
colcon build --symlink-install --packages-select khj_basic
source install/local_setup.bash
```

단축 명령을 사용할 경우:

```bash
cbp khj_basic
sb
```

등록된 실행 파일 확인:

```bash
ros2 pkg executables khj_basic
```

---

# 15. 최종 패키지 구조

Publisher와 Subscriber를 추가한 후 패키지의 주요 구조는 다음과 같다.

```text
khj_basic/
├── khj_basic/
│   ├── __init__.py
│   ├── simple_pub.py
│   ├── class_pub.py
│   └── class_sub.py
├── resource/
│   └── khj_basic
├── test/
├── package.xml
├── setup.cfg
└── setup.py
```

| 파일 | 역할 |
|---|---|
| `simple_pub.py` | 첫 번째 Python 실행 프로그램 |
| `class_pub.py` | `/message` 토픽으로 문자열을 발행하는 노드 |
| `class_sub.py` | `/message` 토픽의 문자열을 구독하는 노드 |
| `setup.py` | 세 Python 파일을 ROS 2 실행 파일로 등록 |
| `setup.cfg` | 실행 파일 설치 경로 설정 |
| `package.xml` | 패키지 정보와 의존성 관리 |

---

# 16. luanch 파일 추가

기존 `khj_basic` 패키지에 `Header` 메시지와 launch 파일을 추가하였다.

```text
khj_basic/
├── khj_basic/
│   ├── simple_pub.py
│   ├── class_pub.py
│   ├── class_sub.py
│   └── header_pub.py
└── launch/
    └── hello.launch.py
```

## 16.1 `Header` 메시지 발행

`header_pub.py`는 `/time` 토픽으로 `Header` 메시지를 발행한다.

```python
from std_msgs.msg import Header
```

```python
self.pub = self.create_publisher(Header, "time", 10)
self.create_timer(1 / 10, self.timer_callback)
```

```python
def timer_callback(self):
    msg = Header()

    msg.stamp = self.get_clock().now().to_msg()
    msg.frame_id = "time"

    self.pub.publish(msg)
```

- 토픽 이름: `/time`
- 메시지 자료형: `std_msgs/msg/Header`
- 발행 주기: 0.1초
- 발행 주파수: 10 Hz

`Header` 메시지는 시간 정보인 `stamp`와 문자열 정보인 `frame_id`를 가진다.

```bash
ros2 topic echo /time
```

---

# 17. Launch 파일 사용

Publisher와 Subscriber를 한 번에 실행하기 위해 다음 launch 파일을 작성하였다.

파일 위치:

```text
khj_basic/launch/hello.launch.py
```

```python
from launch import LaunchDescription
from launch_ros.actions import Node


def generate_launch_description():
    return LaunchDescription(
        [
            Node(
                package="khj_basic",
                executable="class_pub",
            ),
            Node(
                package="khj_basic",
                executable="class_sub",
            ),
        ]
    )
```

Python 패키지에서 launch 파일을 사용하려면 `setup.py`의 `data_files`에 launch 폴더를 등록해야 한다.

```python
from glob import glob
import os
```

```python
(
    "share/" + package_name + "/launch",
    glob(os.path.join("launch", "*.launch.py")),
),
```

빌드 및 실행:

```bash
cd ~/2026_manipulator/khj_ws

colcon build --symlink-install --packages-select khj_basic
source install/local_setup.bash

ros2 launch khj_basic hello.launch.py
```

실행되는 연결 구조:

```text
/message_pub ── /message ──> /message_sub
```

다른 터미널에서 다음 명령을 실행하면 연결 관계를 확인할 수 있다.

```bash
rqt_graph
```

---

# 18. `khj_example_1` 예제 패키지

기존 수업에서 사용한 Publisher, Subscriber, `String`, `Header`, Timer, Logger, launch 파일을 이용하여 새로운 예제 패키지를 만들었다.

```text
khj_example_1/
├── khj_example_1/
│   ├── m_pub.py
│   ├── m_sub.py
│   ├── m2_sub.py
│   ├── t_pub.py
│   └── mt_sub.py
├── launch/
│   └── example.launch.py
├── package.xml
├── setup.cfg
└── setup.py
```

각 노드의 역할은 다음과 같다.

| 노드 | 역할 |
|---|---|
| `/m_pub` | `/message1`, `/message2` 발행 |
| `/m_sub` | `/message1` 구독 |
| `/m2_sub` | `/message2` 구독 |
| `/t_pub` | `/time`을 `Header`로 발행 |
| `/mt_sub` | `/message1`과 `/time` 구독 |

---

# 19. 예제의 통신 구조

| Publisher | 토픽 | 자료형 | Subscriber |
|---|---|---|---|
| `/m_pub` | `/message1` | `String` | `/m_sub`, `/mt_sub` |
| `/m_pub` | `/message2` | `String` | `/m2_sub` |
| `/t_pub` | `/time` | `Header` | `/mt_sub` |

전체 구조:

```text
                      ┌──────────────> /m_sub
                      │
/m_pub ── /message1 ──┤
                      │
                      └──────────────> /mt_sub
                                           ▲
                                           │
/t_pub ──── /time ─────────────────────────┘


/m_pub ──── /message2 ───────────────> /m2_sub
```

이 예제에서 확인할 수 있는 내용은 다음과 같다.

- 하나의 노드에서 여러 토픽을 발행할 수 있다.
- 하나의 토픽을 여러 Subscriber가 구독할 수 있다.
- 하나의 노드에서 여러 토픽을 구독할 수 있다.
- 하나의 노드에서 `String`과 `Header`처럼 서로 다른 메시지 자료형을 사용할 수 있다.

---

# 20. 예제 Launch 파일 실행

`example.launch.py`에서 5개의 노드를 모두 실행한다.

```python
from launch import LaunchDescription
from launch_ros.actions import Node


def generate_launch_description():
    return LaunchDescription(
        [
            Node(package="khj_example_1", executable="m_pub"),
            Node(package="khj_example_1", executable="m_sub"),
            Node(package="khj_example_1", executable="m2_sub"),
            Node(package="khj_example_1", executable="t_pub"),
            Node(package="khj_example_1", executable="mt_sub"),
        ]
    )
```

빌드 및 실행:

```bash
cd ~/2026_manipulator/khj_ws

colcon build --symlink-install --packages-select khj_example_1
source install/local_setup.bash

ros2 launch khj_example_1 example.launch.py
```

노드와 토픽 확인:

```bash
ros2 node list
ros2 topic list
```

예상되는 주요 노드:

```text
/m_pub
/m_sub
/m2_sub
/t_pub
/mt_sub
```

예상되는 주요 토픽:

```text
/message1
/message2
/time
```

`/message1`은 두 노드가 구독하므로 다음 명령에서 Subscription 개수가 2로 표시된다.

```bash
ros2 topic info /message1
```

```text
Publisher count: 1
Subscription count: 2
```

마지막으로 전체 연결 관계를 확인한다.

```bash
rqt_graph
```